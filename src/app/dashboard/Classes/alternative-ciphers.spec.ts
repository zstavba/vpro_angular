import { AlternativeCiphers } from './alternative-ciphers';

describe('AlternativeCiphers', () => {
  it('should create an instance', () => {
    expect(new AlternativeCiphers()).toBeTruthy();
  });
});
