import { WarehouseCategories } from './warehouse-categories';

describe('WarehouseCategories', () => {
  it('should create an instance', () => {
    expect(new WarehouseCategories()).toBeTruthy();
  });
});
